<style type="text/css">
.warning{
    color: red;
}
.maroon{
    color: #700007;
}
</style>

<div class='container hero-unit'>
    <h2>Task Tracker <i class="icon-dashboard"></i></h2>
    <h3>Login</h3>
    <form>
        <label><i class="icon-envelope"></i> Email <span id="loginWarning" class="warning"></label>
        <input id="loginEmail" type="text" name="email">
        <label><i class="icon-key"></i> Password</label>
        <input id="loginPassword" type="password" name="password">
    </form>
    <button id="loginButton" class="btn btn-primary" >Login <i class="icon-signin"></i></button>

    <h3>Register</h3>
    <form>
        <label><i class="icon-envelope"></i> Email<span id="registerWarning" class="warning"></span></label>
        <input id="registerEmail" type="text" name="email">
        <label><i class="icon-key"></i> Password<span id="registerPasswordWarning" class="warning"></label>
        <input id="registerPassword1"type="password" name="password">
        <label><i class="icon-key"></i> Verify Password</label>
        <input id="registerPassword2" type="password" name="verifypassword">
    </form>
    <button id="registerButton" class="btn btn-primary" >Register <i class="icon-edit"></i></button>
</div>
<script type="text/javascript">
/*$(document).ready(function (){
    
    $('#loginButton').click(function(){
        if(($('#loginEmail').val() == "") || ($('#loginPassword').val() == "")){
            $('#loginWarning').text("      To login you must enter your E-Mail address and Password");       
            setTimeout(ClearWarning, 5000);
        }
        else
        {
            var loginObject = {
                email: $('#loginEmail').val(),
                password: $('#loginPassword').val()
            }
            var success = SendLoginData(loginObject);
            if(success){
                alert("goto task list page");
            }
            else
            {
                $('#loginWarning').text(" Incorrect E-Mail address or Password");       
                setTimeout(ClearWarning, 5000);
            }
//            alert($('#loginEmail').val()+" login clicked " + loginObject.email + " " + loginObject.password);
        }
    });

    $('#registerButton').click(function(){
        if(($('#registerEmail').val() == "") || ($('#registerPassword1').val() == "")|| ($('#registerPassword2').val() == "")){
             $('#registerWarning').text("      To Register you must fill in all the registration fields, E-Mail address, Password, and Confirm Password");
              setTimeout(ClearWarning, 5000);
            //alert("To Register you must fill in all the registration fields, E-Mail address, Password, and Verify Password");
        }
        else
        {
            if($('#registerPassword1').val() == $('#registerPassword2').val())
            {
                var registerObject = {
                    email: $('#registerEmail').val(),
                    password: $('#registerPassword1').val()
                }
                SendRegisterData(registerObject);
                //alert("register clicked "+ registerObject.email + " " + registerObject.password);
            }
            else
            {
                $('#registerPasswordWarning').text("      your passwords do not match");
                setTimeout(ClearWarning, 5000);
               
            }
        }
        
    });

});

function SendLoginData(loginData){
    alert("email: " + loginData.email + " password: " + loginData.password);
    var success = false;
    return success;
}

function SendRegisterData(registerData){
    alert("email: " + registerData.email + " password: " + registerData.password);
}
function ClearWarning() {
    $('.warning').text("");
}*/

</script>

